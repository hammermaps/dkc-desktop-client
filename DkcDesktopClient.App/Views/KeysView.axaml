<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DkcDesktopClient.App.ViewModels"
             x:Class="DkcDesktopClient.App.Views.KeysView"
             x:DataType="vm:KeysViewModel">
  <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto, *" Margin="16" RowSpacing="8">

    <!-- Toolbar -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
      <TextBlock Text="Keys Management" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
      <Button Content="Aktualisieren" Command="{Binding LoadDataCommand}"/>
      <Separator Width="1" Background="#CCCCCC" Margin="4,0"/>
      <Button Content="Neu" Command="{Binding ShowCreateKeyFormCommand}"/>
      <Button Content="Bearbeiten" Command="{Binding ShowEditKeyFormCommand}"/>
      <Button Content="Ausgeben" Command="{Binding ShowIssueFormCommand}"/>
      <Separator Width="1" Background="#CCCCCC" Margin="4,0"/>
      <Button Content="🔄 Zurückgeben" Command="{Binding ShowReturnFormCommand}"/>
      <Button Content="Löschen" Command="{Binding DeleteIssuedCommand}" Foreground="Red"/>
      <Separator Width="1" Background="#CCCCCC" Margin="4,0"/>
      <Button Content="📥 CSV-Export" Command="{Binding ExportInventoryToCsvCommand}"
              ToolTip.Tip="Schlüssel-Inventar als CSV exportieren"/>
    </StackPanel>

    <!-- Stats tiles -->
    <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,0">
      <Border Background="#90CDF4" BorderBrush="#63B3ED" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,8,0">
        <StackPanel HorizontalAlignment="Center">
          <TextBlock Text="Schlüsseltypen" FontSize="12" Foreground="#4A5568" HorizontalAlignment="Center"/>
          <TextBlock Text="{Binding StatTotalKeyTypes}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>
      </Border>
      <Border Background="#9AE6B4" BorderBrush="#68D391" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,8,0">
        <StackPanel HorizontalAlignment="Center">
          <TextBlock Text="Gesamt" FontSize="12" Foreground="#4A5568" HorizontalAlignment="Center"/>
          <TextBlock Text="{Binding StatTotalKeys}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>
      </Border>
      <Border Background="#FBD38D" BorderBrush="#F6AD55" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,8,0">
        <StackPanel HorizontalAlignment="Center">
          <TextBlock Text="Ausgegeben" FontSize="12" Foreground="#4A5568" HorizontalAlignment="Center"/>
          <TextBlock Text="{Binding StatIssuedKeys}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>
      </Border>
      <Border Background="#D6BCFA" BorderBrush="#B794F4" BorderThickness="1" CornerRadius="4" Padding="12">
        <StackPanel HorizontalAlignment="Center">
          <TextBlock Text="Verfügbar" FontSize="12" Foreground="#4A5568" HorizontalAlignment="Center"/>
          <TextBlock Text="{Binding StatAvailableKeys}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>
      </Border>
    </UniformGrid>

    <!-- Error / Progress -->
    <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red"
               IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
    <ProgressBar Grid.Row="2" IsIndeterminate="True" IsVisible="{Binding IsLoading}" Height="4" VerticalAlignment="Bottom"/>

    <!-- Key create/edit form -->
    <Border Grid.Row="3" IsVisible="{Binding IsKeyFormVisible}"
            Background="#CBD5E0" BorderThickness="1" BorderBrush="#CBD5E0" CornerRadius="4" Padding="12">
      <StackPanel Spacing="8">
        <TextBlock Text="Key Type" FontSize="15" FontWeight="SemiBold"/>
        <TextBlock Text="{Binding KeyFormError}" Foreground="Red"
                   IsVisible="{Binding KeyFormError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
        <Grid ColumnDefinitions="100,8,*,16,80,8,Auto" RowDefinitions="Auto,8,Auto" RowSpacing="0">
          <TextBlock Grid.Row="0" Grid.Column="0" Text="Name *" VerticalAlignment="Center"/>
          <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding FormKeyName}" PlaceholderText="Key name (required)"/>
          <TextBlock Grid.Row="0" Grid.Column="4" Text="Total" VerticalAlignment="Center"/>
          <NumericUpDown Grid.Row="0" Grid.Column="6" Value="{Binding FormKeyTotal}" Minimum="1" Maximum="999" Width="80"/>
          <TextBlock Grid.Row="2" Grid.Column="0" Text="Description" VerticalAlignment="Top" Margin="0,4,0,0"/>
          <TextBox Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="5" Text="{Binding FormKeyDescription}"
                   PlaceholderText="Optional description" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60"/>
        </Grid>
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Content="Save" Command="{Binding SaveKeyCommand}"/>
          <Button Content="Cancel" Command="{Binding CancelKeyFormCommand}"/>
          <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsSavingKey}" Width="80" Height="16" VerticalAlignment="Center"/>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- Issue form -->
    <Border Grid.Row="4" IsVisible="{Binding IsIssueFormVisible}"
            Background="#FAF089" BorderThickness="1" BorderBrush="#F6C90E" CornerRadius="4" Padding="12">
      <StackPanel Spacing="8">
        <TextBlock Text="{Binding SelectedInventoryItem.Name, StringFormat='Ausgeben: {0}'}" FontSize="15" FontWeight="SemiBold"/>
        <TextBlock Text="{Binding IssueFormError}" Foreground="Red"
                   IsVisible="{Binding IssueFormError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
        <Grid ColumnDefinitions="100,8,*,16,100,8,*" RowDefinitions="Auto,8,Auto" RowSpacing="0">
          <TextBlock Grid.Row="0" Grid.Column="0" Text="Empfänger *" VerticalAlignment="Center"/>
          <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding FormIssuedTo}" PlaceholderText="Personenname (Pflichtfeld)"/>
          <TextBlock Grid.Row="0" Grid.Column="4" Text="Datum *" VerticalAlignment="Center"/>
          <TextBox Grid.Row="0" Grid.Column="6" Text="{Binding FormIssuedAt}" PlaceholderText="YYYY-MM-DD"/>
          <TextBlock Grid.Row="2" Grid.Column="0" Text="Notizen" VerticalAlignment="Top" Margin="0,4,0,0"/>
          <TextBox Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="5" Text="{Binding FormIssueNotes}"
                   PlaceholderText="Optionale Notizen" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60"/>
        </Grid>
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Content="Ausgeben" Command="{Binding IssueKeyCommand}"/>
          <Button Content="Abbrechen" Command="{Binding CancelIssueFormCommand}"/>
          <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsSavingIssue}" Width="80" Height="16" VerticalAlignment="Center"/>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- Return form -->
    <Border Grid.Row="4" IsVisible="{Binding IsReturnFormVisible}"
            Background="#C6F6D5" BorderThickness="1" BorderBrush="#68D391" CornerRadius="4" Padding="12">
      <StackPanel Spacing="8">
        <TextBlock Text="{Binding SelectedIssuedItem.KeyName, StringFormat='Rückgabe: {0}'}" FontSize="15" FontWeight="SemiBold"/>
        <TextBlock Text="{Binding ReturnFormError}" Foreground="Red"
                   IsVisible="{Binding ReturnFormError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
        <Grid ColumnDefinitions="130,8,*,16,130,8,*">
          <TextBlock Grid.Column="0" Text="Rückgabedatum *" VerticalAlignment="Center"/>
          <TextBox   Grid.Column="2" Text="{Binding FormReturnDate}" PlaceholderText="YYYY-MM-DD"/>
          <TextBlock Grid.Column="4" Text="Notizen" VerticalAlignment="Top" Margin="0,4,0,0"/>
          <TextBox   Grid.Column="6" Text="{Binding FormReturnNotes}"
                     PlaceholderText="Optionale Notizen" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60"/>
        </Grid>
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Content="Rückgabe bestätigen" Command="{Binding ReturnKeyCommand}"/>
          <Button Content="Abbrechen" Command="{Binding CancelReturnFormCommand}"/>
          <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsSavingReturn}" Width="80" Height="16" VerticalAlignment="Center"/>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- Tabs -->
    <TabControl Grid.Row="5" SelectedIndex="{Binding SelectedTabIndex}">
      <TabItem Header="Inventory">
        <DataGrid ItemsSource="{Binding Inventory}" SelectedItem="{Binding SelectedInventoryItem}"
                  IsReadOnly="True" AutoGenerateColumns="False">
          <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="70"/>
            <DataGridTextColumn Header="Available" Binding="{Binding Available}" Width="80"/>
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>
      <TabItem Header="Issued Keys">
        <DataGrid ItemsSource="{Binding IssuedKeys}" SelectedItem="{Binding SelectedIssuedItem}"
                  IsReadOnly="True" AutoGenerateColumns="False">
          <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
            <DataGridTextColumn Header="Key Name" Binding="{Binding KeyName}" Width="*"/>
            <DataGridTextColumn Header="Issued To" Binding="{Binding IssuedTo}" Width="*"/>
            <DataGridTextColumn Header="Issued At" Binding="{Binding IssuedAt}" Width="130"/>
            <DataGridTextColumn Header="Returned At" Binding="{Binding ReturnedAt}" Width="130"/>
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>
    </TabControl>

  </Grid>
</UserControl>
